# v4.5 访问与用量限制

现有旅人名、四位暗号、Supabase 表和存档保持兼容。升级后重新登录一次。
登录成功后签发 7 天有效、HttpOnly/Secure/SameSite=Strict 的 Cookie。网页需 HTTPS。
服务器验证账户和会话归属；不再把浏览器 localStorage 或 player_id 当作身份证明。
Supabase 不可用时拒绝登录，不再降级成任意名字登录。

默认限制：
- 单条聊天和语音文本最多 500 字，请求体最多 8 KiB。
- 聊天每账户每分钟 6 次、每天 100 次；语音每分钟 12 次、每天 100 次。
- 全站实际大模型请求每天 500 次，含重试、改写和记忆提取；语音供应商请求每天 200 次，含备用供应商。
- 每个账户名每天最多 20 次登录尝试；连接来源每分钟最多 30 次。来源取服务器连接地址，不信任任意 X-Forwarded-For，因此代理后的用户可能共享该限制。
- 每天最多新增 10 个账户；其他已登录 API 每账户每分钟 60 次。
- 上下文最近消息合计最多 6000 字，系统提示最多 12000 字；历史长消息截到 500 字。
- 每日计数以北京时间零点为边界；失败的尝试也占额度，不自动退款计数。

可选环境变量：AUTH_SECRET（独立签名密钥）；LLM_DAILY_LIMIT（默认 500）；TTS_DAILY_LIMIT（默认 200）；SECURITY_DB_PATH（默认 security.sqlite3）。未设置 AUTH_SECRET 时从 DeepSeek Key 派生签名密钥，轮换 Key 会令旧登录失效。

限制适用于当前单实例部署。SQLite 事务保障并发扣减，同一磁盘上的 worker 重启不清零；Render 免费实例重建、重新部署或磁盘丢失可能清零。多实例或严格跨部署预算需要共享持久计数服务，不能把当前限制当人民币账单封顶。保持单 worker（游戏会话本身也是进程内状态），不要靠增加 worker 扩容。

日志仅记录请求 ID、账户 ID 的哈希、路径、状态码；不记录聊天正文、密钥、暗号或原始 IP。
四位暗号仍是低强度的旧机制，限次只能减少在线猜测风险；这次没有迁移原数据库中的暗号存储格式。
撤销曾泄露的供应商 Key 仍然必要：应用限制无法约束绕过本应用的直接 API 调用。

验证：python -m unittest discover -s tests -p test_access_control.py -v
测试使用模拟 Supabase/模型/语音服务，不产生付费调用。
